# Configuración para desarrollo local con Docker
# Copia este archivo a .env y ajusta los valores según necesites

# Para usar la base de datos local de Docker (por defecto)
# Deja estas variables sin comentar:
DB_HOST=postgres
DB_PORT=5432
DB_NAME=catalogo_productos
DB_USER=postgres
DB_PASSWORD=postgres

# Para usar la base de datos de Render localmente:
# 1. Comenta las variables DB_* de arriba
# 2. Descomenta y configura DATABASE_URL con tu conexión de Render:
# DATABASE_URL=postgresql://usuario:password@host:puerto/database?sslmode=require

# Ejemplo de DATABASE_URL de Render:
# DATABASE_URL=postgresql://catalogo_user:password123@dpg-xxxxx-a.oregon-postgres.render.com:5432/catalogo_productos?sslmode=require

# Configuración del API
PORT=3001
API_VERSION=v1

# Configuración del Frontend
FRONTEND_PORT=3000
API_URL=http://localhost:3001

# Cloudinary (opcional, para producción)
# CLOUDINARY_CLOUD_NAME=tu_cloud_name
# CLOUDINARY_API_KEY=tu_api_key
# CLOUDINARY_API_SECRET=tu_api_secret

